______________________________________________
Squared2 Server

Dev Server Setup
1. install MongoDB on your system
   find the download here: https://www.mongodb.com/
2. run `npm i` in \backend
3. launch server with `npm run dev` in \backend

